name: blank

on:
  push:

jobs:
  focus:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        Europe: ["tremble", "invade", "normal", "November", "comprehensive"]
        keeper: ["cordial", "explain", "born", "gauge"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar pd
